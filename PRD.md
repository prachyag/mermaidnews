# PRD — News Curator: ระบบรวบรวมข่าวตามหัวข้อที่สนใจและโพสลง Facebook

| | |
|---|---|
| **ชื่อโปรเจกต์** | News Curator (ชื่อชั่วคราว — use case แรก: ข่าวนางเงือก) |
| **เวอร์ชันเอกสาร** | 1.1 |
| **วันที่** | 16 กรกฎาคม 2026 |
| **สถานะ** | Draft |

---

## 1. ภาพรวม (Overview)

Web app ที่ค้นหาและรวบรวมข่าวตาม**หัวข้อ (Topic) ที่ผู้ใช้กำหนดเอง** โดยอัตโนมัติในแต่ละวันหรือตามช่วงเวลาที่กำหนด จากนั้นใช้ AI คัดกรองความเกี่ยวข้องและเรียบเรียงเป็นโพสต์ภาษาไทยพร้อมโพส แอดมินตรวจสอบ/แก้ไขผ่านหน้า Dashboard แล้วสั่งโพสลง Facebook Page ได้ทันทีหรือตั้งเวลาโพส

ระบบออกแบบเป็น **generic** — ไม่ผูกกับเรื่องใดเรื่องหนึ่ง ผู้ใช้สร้างหัวข้อได้หลายหัวข้อและสลับ/เพิ่ม/ปิดได้ตลอด เช่น "นางเงือก", "แมวจร", "รถไฟฟ้าสายสีส้ม" — **หัวข้อแรกที่จะใช้งานจริงคือ "นางเงือก"** ซึ่งจะเป็นค่า seed เริ่มต้นของระบบ

### ปัญหาที่ต้องการแก้ (Problem Statement)

- การติดตามข่าวเฉพาะเรื่องที่สนใจต้องไล่ค้นเองจากหลายแหล่งทุกวัน เสียเวลา
- ข่าวที่ค้นเจอด้วย keyword มักปนหลายบริบทที่ไม่เกี่ยวข้อง ต้องคัดกรองเอง
- การเรียบเรียงเป็นโพสต์ภาษาไทยและโพสลงเพจเป็นงานซ้ำ ๆ ที่ทำมือทุกครั้ง
- เครื่องมือที่มีมักผูกกับหัวข้อเดียว เปลี่ยนเรื่องที่ติดตามไม่ได้โดยไม่แก้โค้ด

### เป้าหมาย (Goals)

1. **ผู้ใช้สร้างและจัดการหัวข้อข่าว (Topic) ได้เองจากหน้าเว็บ** — กำหนดชื่อ, keyword, เงื่อนไขความเกี่ยวข้อง โดยไม่ต้องแก้โค้ด
2. ระบบดึงข่าวอัตโนมัติตาม schedule ทุกวัน สำหรับทุกหัวข้อที่เปิดใช้งาน
3. **ผู้ใช้สามารถกดปุ่ม "ดึงข่าวทันที" (Fetch Now)** ได้ทุกเมื่อ — ทั้งแบบทุกหัวข้อ หรือเลือกเฉพาะหัวข้อ
4. AI คัดกรองข่าวที่เกี่ยวข้องจริงตามบริบทของแต่ละหัวข้อ และร่างแคปชันภาษาไทยให้อัตโนมัติ
5. โพสลง Facebook Page ได้จาก Dashboard ภายใน 2–3 คลิก
6. ไม่มีข่าวซ้ำ — ทั้งซ้ำในระบบและซ้ำกับที่เคยโพสไปแล้ว

### สิ่งที่อยู่นอกขอบเขต (Non-Goals) — เวอร์ชันแรก

- โพสลงโปรไฟล์ Facebook ส่วนตัว (Graph API ไม่รองรับ — รองรับเฉพาะ Page)
- โพสลงแพลตฟอร์มอื่น (X, Instagram, TikTok)
- ~~ระบบหลายผู้ใช้~~ → **ปรับขอบเขตแล้ว (ก.ค. 2026): รองรับหลายบัญชี** — สมัคร/ล็อกอิน/ล็อกเอาต์ได้ หัวข้อ ข่าว และการตั้งค่าต่อหัวข้อแยกตามบัญชี (ยกเว้น Gemini API key และ Facebook token ที่เป็นค่ากลางระดับระบบใน env)
- วิเคราะห์ยอด engagement ของโพสต์
- สร้างรูปภาพประกอบด้วย AI

---

## 2. ผู้ใช้งาน (Users)

**แอดมินเพจ (ผู้ใช้หลักคนเดียว)** — เจ้าของ/แอดมิน Facebook Page ที่ต้องการคอนเทนต์ข่าวเฉพาะเรื่องลงเพจสม่ำเสมอ ใช้งานผ่าน browser บน desktop เป็นหลัก

---

## 3. แนวคิดหลัก: หัวข้อข่าว (Topic)

Topic คือหน่วยกลางของระบบ — ทุกการดึงข่าว การคัดกรอง และการโพส ผูกกับ Topic เสมอ

แต่ละ Topic ประกอบด้วย:

| ฟิลด์ | คำอธิบาย | ตัวอย่าง (หัวข้อ "นางเงือก") |
|---|---|---|
| ชื่อหัวข้อ | ชื่อที่แสดงบน Dashboard | นางเงือก |
| Keywords | คำค้นหลายคำ ทั้งไทย/อังกฤษ | นางเงือก, mermaid, little mermaid |
| คำอธิบายบริบท (AI context) | ข้อความบอก AI ว่าข่าวแบบไหน "เกี่ยวข้อง" และแบบไหนไม่ใช่ | "ข่าวเกี่ยวกับนางเงือกทุกแง่มุม เช่น หนัง การแสดง ตำนาน — ไม่รวมข่าวที่แค่มีชื่อสถานที่/ร้านที่มีคำว่านางเงือก" |
| สไตล์แคปชัน | โทน/รูปแบบโพสต์ที่ให้ AI ใช้ | เป็นกันเอง มีอีโมจิ ลงท้ายด้วยแฮชแท็ก |
| Facebook Page ปลายทาง | เพจที่จะโพสข่าวของหัวข้อนี้ | เพจนางเงือกนิวส์ |
| Schedule | เวลาดึงอัตโนมัติของหัวข้อนี้ | ทุกวัน 07:00 |
| สถานะ | เปิด/ปิดการใช้งาน (ปิดแล้วข้อมูลเดิมยังอยู่) | เปิด |

- ระบบ seed หัวข้อ "นางเงือก" มาให้เป็นตัวอย่างเริ่มต้น
- หัวข้อต่างกันโพสลงเพจต่างกันได้ (หรือใช้เพจเดียวกันก็ได้)

---

## 4. User Stories

| # | User Story | Priority |
|---|---|---|
| US-1 | ในฐานะแอดมิน ฉันต้องการ**สร้าง/แก้ไข/ปิดหัวข้อข่าวได้เอง** (ชื่อ, keyword, บริบท AI) เพื่อเปลี่ยนเรื่องที่ติดตามได้โดยไม่ต้องแก้โค้ด | Must |
| US-2 | ในฐานะแอดมิน ฉันต้องการให้ระบบดึงข่าวของทุกหัวข้อที่เปิดใช้อัตโนมัติทุกเช้า | Must |
| US-3 | ในฐานะแอดมิน ฉันต้องการ**กดปุ่มดึงข่าวได้ทันที** ทั้งแบบทุกหัวข้อหรือเลือกเฉพาะหัวข้อ | Must |
| US-4 | ในฐานะแอดมิน ฉันต้องการเห็นรายการข่าวแยกตามหัวข้อและตามวัน/ช่วงเวลา พร้อมสถานะ | Must |
| US-5 | ในฐานะแอดมิน ฉันต้องการให้ AI คัดกรองตามบริบทของหัวข้อนั้น ๆ และร่างแคปชันภาษาไทยตามสไตล์ที่ตั้งไว้ | Must |
| US-6 | ในฐานะแอดมิน ฉันต้องการแก้ไขแคปชันก่อนโพส | Must |
| US-7 | ในฐานะแอดมิน ฉันต้องการกดโพสลงเพจปลายทางของหัวข้อนั้นทันที หรือตั้งเวลาโพส | Must |
| US-8 | ในฐานะแอดมิน ฉันต้องการดูประวัติโพสต์ที่โพสไปแล้ว พร้อมลิงก์ไปยังโพสต์จริง | Should |
| US-9 | ในฐานะแอดมิน ฉันต้องการกดปฏิเสธ (reject) ข่าวที่ไม่อยากโพส เพื่อไม่ให้กลับมาโผล่อีก | Should |
| US-10 | ในฐานะแอดมิน ฉันต้องการทดลองหัวข้อใหม่ (ดึงครั้งเดียวดูผลก่อน) ก่อนตัดสินใจเปิด schedule | Could |

---

## 5. Functional Requirements

### 5.1 Topic Management (จัดการหัวข้อ)

- **FR-1.1** สร้าง/แก้ไข/เปิด-ปิดหัวข้อได้จากหน้า Settings — ฟิลด์ตามตารางในข้อ 3
- **FR-1.2** หัวข้อหนึ่งมีได้หลาย keyword (ไทย/อังกฤษปนกันได้) แต่ละ keyword กลายเป็น query แยกตอนดึงข่าว
- **FR-1.3** ปิดหัวข้อ = หยุดดึงและหยุดประมวลผล แต่ข่าวและประวัติโพสต์เดิมยังดูได้
- **FR-1.4** ลบหัวข้อได้เฉพาะเมื่อยืนยันซ้ำ และต้องเตือนว่าข่าวใต้หัวข้อจะถูกลบด้วย
- **FR-1.5** ระบบ seed หัวข้อ "นางเงือก" (keywords: นางเงือก, mermaid) เป็นค่าเริ่มต้นตอนติดตั้ง

### 5.2 News Fetcher (ตัวดึงข่าว)

- **FR-2.1** ดึงข่าวจาก Google News RSS ตาม keyword ของแต่ละหัวข้อที่เปิดใช้งาน
- **FR-2.2** รันอัตโนมัติตาม schedule ของแต่ละหัวข้อ (ค่าเริ่มต้น: ทุกวัน 07:00 น. เวลาไทย)
- **FR-2.3 — Fetch Now:** มีปุ่ม **"ดึงข่าวทันที"** บน Dashboard
  - เลือกได้ว่าดึง**ทุกหัวข้อ**หรือ**เฉพาะหัวข้อที่เลือก**
  - ระหว่างดึงต้องแสดงสถานะกำลังทำงาน (loading/progress) และปิดปุ่มกันกดซ้ำ
  - เมื่อเสร็จ แสดงผลสรุปต่อหัวข้อ เช่น "นางเงือก: พบข่าวใหม่ 5 รายการ (ข้าม 12 ที่ซ้ำ)"
  - ถ้าหัวข้อใดมีการดึง (schedule หรือ manual) ค้างอยู่ ต้องไม่เริ่มรอบใหม่ซ้อนของหัวข้อนั้น (lock ต่อหัวข้อ)
  - ผลลัพธ์เข้า pipeline เดียวกับการดึงตาม schedule ทุกประการ (dedupe → AI → draft)
- **FR-2.4** กันข่าวซ้ำ (dedupe) ภายในหัวข้อเดียวกัน ด้วย URL ปกติแล้ว (normalized URL) และหัวข้อข่าวที่คล้ายกันมาก
- **FR-2.5** เก็บข้อมูลต่อข่าว: หัวข้อ (topic), หัวข้อข่าว, URL ต้นทาง, สำนักข่าว, วันเวลาเผยแพร่, เนื้อหาย่อ/description, รูป (og:image ถ้ามี), วันเวลาที่ดึง
- **FR-2.6** บันทึก log ทุกรอบการดึง: หัวข้อ, เวลาเริ่ม–จบ, ที่มา (schedule/manual), จำนวนที่พบ/ใหม่/ซ้ำ/ผิดพลาด

### 5.3 AI Processing (คัดกรอง + ร่างโพสต์)

- **FR-3.1** ใช้ AI (Gemini API ผ่าน `AiProvider` interface) ประเมินข่าวใหม่แต่ละชิ้นว่าเกี่ยวข้องกับหัวข้อจริงหรือไม่ โดยใช้**คำอธิบายบริบท (AI context) ของหัวข้อนั้น**เป็นเกณฑ์ พร้อมคะแนนความเกี่ยวข้อง (relevance score) — ข่าวที่ไม่ผ่านถูกทำเครื่องหมาย "ไม่เกี่ยวข้อง" (ยังดูย้อนหลังได้ ไม่ลบทิ้ง)
- **FR-3.2** ข่าวที่ผ่านเกณฑ์ ให้ AI สร้าง: สรุปข่าวภาษาไทยสั้น ๆ, แคปชันสำหรับโพสตาม**สไตล์แคปชันของหัวข้อ**, แฮชแท็ก, และแนบลิงก์ข่าวต้นทาง
- **FR-3.3** โพสต์ที่ AI ร่างมีสถานะ **Draft** เสมอ — ระบบไม่โพสเองโดยไม่ผ่านการอนุมัติ (เวอร์ชันแรก)
- **FR-3.4** ถ้าเรียก AI ล้มเหลว ข่าวคงสถานะ "รอประมวลผล" และมีปุ่มสั่งประมวลผลซ้ำ

### 5.4 Dashboard

- **FR-4.1** หน้าแรกแสดงรายการข่าว เรียงใหม่→เก่า **กรองตามหัวข้อ (topic)**, ตามวัน/ช่วงเวลา และตามสถานะได้ (ใหม่ / Draft / อนุมัติแล้ว / โพสแล้ว / ปฏิเสธ / ไม่เกี่ยวข้อง) — แต่ละข่าวแสดง badge หัวข้อชัดเจน
- **FR-4.2** ปุ่ม "ดึงข่าวทันที" อยู่ตำแหน่งเด่นบนหน้าแรก พร้อม dropdown เลือกหัวข้อ (ค่าเริ่มต้น: ทุกหัวข้อ) และแสดงเวลาดึงล่าสุด + รอบ schedule ถัดไปของแต่ละหัวข้อ
- **FR-4.3** หน้ารายละเอียดข่าว: เนื้อหาต้นทาง, แคปชันที่ AI ร่าง (แก้ไขได้), รูปตัวอย่าง, ปุ่ม อนุมัติ+โพสทันที / ตั้งเวลาโพส / ปฏิเสธ
- **FR-4.4** หน้าประวัติ: โพสต์ที่โพสแล้ว (กรองตามหัวข้อได้) พร้อมลิงก์ไปโพสต์จริงบน Facebook และเวลาที่โพส
- **FR-4.5** หน้า Settings: จัดการหัวข้อ (ตาม 5.1), การเชื่อมต่อ Facebook, เกณฑ์ relevance score

### 5.5 Facebook Publisher

- **FR-5.1** โพสลง Facebook Page **ปลายทางของหัวข้อนั้น** ผ่าน Graph API (`POST /{page-id}/feed` หรือ `/photos` เมื่อมีรูป) ด้วย Page Access Token — **Page ID และ Access Token เก็บต่อหัวข้อ** (ตั้งในหน้าจัดการหัวข้อของแต่ละบัญชี ไม่ใช่ค่ากลางระดับระบบ) เพื่อให้แต่ละบัญชีใช้เพจ/token ของตัวเอง; token ไม่ถูกส่งกลับไป client (คืนแค่ธง hasFbToken)
- **FR-5.2** รองรับโพสทันทีและตั้งเวลาโพส (ใช้ scheduled publishing ของ Graph API เป็นหลัก)
- **FR-5.3** บันทึก Facebook post ID และลิงก์โพสต์หลังโพสสำเร็จ
- **FR-5.4** ถ้าโพสล้มเหลว แสดง error ชัดเจนบน Dashboard และให้กด retry ได้ — ห้าม retry อัตโนมัติแบบไม่จำกัด (กันโพสซ้ำ)
- **FR-5.5** กันการโพสข่าวเดียวกันซ้ำ: ข่าวที่มีสถานะ "โพสแล้ว" จะโพสซ้ำได้ต่อเมื่อยืนยันอีกครั้งเท่านั้น

---

## 6. สถานะของข่าว (Content Lifecycle)

```
ดึงเข้ามา (fetched)
   └─ AI คัดกรองตามบริบทหัวข้อ ──> ไม่เกี่ยวข้อง (irrelevant) [จบ]
        └─ ผ่าน ──> ร่างโพสต์ (draft)
              ├─ แอดมินปฏิเสธ ──> rejected [จบ]
              └─ แอดมินอนุมัติ ──> approved
                    ├─ โพสทันที ──> posted [จบ]
                    └─ ตั้งเวลา ──> scheduled ──> posted [จบ]
                                        └─ โพสล้มเหลว ──> failed (retry ได้)
```

---

## 7. Non-Functional Requirements

- **NFR-1** การดึงข่าวหนึ่งรอบต่อหัวข้อ ควรเสร็จภายใน 2 นาที
- **NFR-2** ปุ่ม Fetch Now ต้องตอบสนอง (แสดงสถานะเริ่มทำงาน) ภายใน 1 วินาที — การดึงจริงทำงานเบื้องหลัง ไม่ block หน้าเว็บ
- **NFR-3** Secrets ทั้งหมด (Claude API key, Facebook token) เก็บใน environment variables — ห้าม hardcode และห้าม commit ลง git
- **NFR-4** Dashboard และ API ทั้งหมดต้องล็อกอินก่อนใช้งาน — ระบบบัญชี username/password (scrypt hash) + session cookie แบบ HMAC-signed อายุ 30 วัน, ข้อมูลทุกอย่าง scope ตามบัญชีทั้งระดับ UI และระดับ query. ปิดรับสมัครสมาชิกใหม่ได้ด้วย env `ALLOW_REGISTRATION=false` (บัญชีแรกของระบบยังสมัครได้เสมอเพื่อ bootstrap)
- **NFR-5** ระบบ log เพียงพอต่อการไล่ปัญหา: ทุกรอบ fetch, ทุกครั้งที่เรียก AI, ทุกครั้งที่โพส
- **NFR-6** ภาษาของ UI: ไทย
- **NFR-7** การเพิ่มหัวข้อใหม่ต้องไม่ต้องแก้โค้ดหรือ restart ระบบ

---

## 8. Technical Design (สรุป)

### Stack

| ส่วน | เทคโนโลยี |
|---|---|
| Web app + API | Next.js (App Router, TypeScript) |
| ฐานข้อมูล | libSQL ผ่าน Drizzle ORM — dev ใช้ไฟล์ SQLite ในเครื่อง, production ใช้ **Turso** (SQLite-compatible cloud) เพราะ Vercel เป็น serverless เก็บไฟล์ SQLite ไม่ได้ |
| Scheduler | **Vercel Cron** (node-cron ใช้บน Vercel ไม่ได้เพราะไม่มี process ค้างตลอด) |
| Deploy | **Vercel** |
| แหล่งข่าว | Google News RSS (ฟรี ไม่ต้องใช้ key) — เผื่อขยายเป็น NewsAPI/GNews ภายหลัง |
| AI | **Gemini API** (Google AI Studio — มี free tier) เรียกผ่าน interface กลาง `AiProvider` เพื่อให้สลับไป Claude/เจ้าอื่นได้โดยแก้โค้ดจุดเดียว |
| โพส Facebook | Meta Graph API (Page Access Token, สิทธิ์ `pages_manage_posts`) |

> ข้อจำกัด serverless ที่ต้องออกแบบเผื่อ: function timeout (ดึงข่าวเป็นชุดเล็ก ๆ), lock กันดึงซ้อนต้องอยู่ในฐานข้อมูล (in-memory lock ใช้ข้าม instance ไม่ได้)

### API Endpoints หลัก (ภายในแอป)

| Method | Path | หน้าที่ |
|---|---|---|
| `GET/POST` | `/api/topics` | รายการ/สร้างหัวข้อ |
| `PATCH/DELETE` | `/api/topics/:id` | แก้ไข/เปิด-ปิด/ลบหัวข้อ |
| `POST` | `/api/fetch` | **สั่งดึงข่าวทันที (ปุ่ม Fetch Now)** — body ระบุ topic id หรือ `all`; ตอบกลับ job id ทันที, ดึงจริงเบื้องหลัง |
| `GET` | `/api/fetch/status` | สถานะการดึงรอบปัจจุบัน/ล่าสุด แยกต่อหัวข้อ (ใช้ให้ปุ่มแสดง progress) |
| `GET` | `/api/articles` | รายการข่าว (filter ตามหัวข้อ/วัน/สถานะ) |
| `PATCH` | `/api/articles/:id` | แก้แคปชัน / เปลี่ยนสถานะ (อนุมัติ, ปฏิเสธ) |
| `POST` | `/api/articles/:id/publish` | โพสลง Facebook (ทันทีหรือตั้งเวลา) |
| `POST` | `/api/articles/:id/reprocess` | สั่ง AI ประมวลผลซ้ำ |

### Data Model (หลัก)

- **Topic** — id, name, keywords (list), aiContext, captionStyle, fbPageId, cronSchedule, enabled, createdAt
- **Article** — id, topicId (FK), title, url, source, publishedAt, description, imageUrl, status, relevanceScore, summary, caption, hashtags, fbPostId, fbPostUrl, postedAt, scheduledAt, createdAt — unique (topicId, url)
- **FetchRun** — id, topicId (FK), trigger (`schedule` | `manual`), startedAt, finishedAt, found, new, duplicates, errors, status
- **Setting** — key, value (ค่ากลางของระบบ เช่น เกณฑ์ relevance score, Facebook app credentials)

---

## 9. แผนการพัฒนา (Milestones)

| เฟส | ขอบเขต | Definition of Done |
|---|---|---|
| **1. Topic + Fetcher + Fetch Now** | ตั้งโปรเจกต์, data model Topic/Article, seed หัวข้อ "นางเงือก", ดึงข่าวจาก Google News RSS ตาม keyword ของหัวข้อ, dedupe, หน้า list ข่าวแบบง่าย + **ปุ่มดึงข่าวทันที (เลือกหัวข้อได้)** | สร้างหัวข้อใหม่ → กดปุ่ม → เห็นข่าวของหัวข้อนั้นในหน้ารายการ ไม่มีข่าวซ้ำ |
| **2. AI Processing** | ต่อ Gemini API (ผ่าน `AiProvider` interface) คัดกรองตาม aiContext ของหัวข้อ + ร่างแคปชันตาม captionStyle | ข่าวใหม่ทุกชิ้นถูกคัดกรองและมีแคปชันร่างอัตโนมัติตามสไตล์ของหัวข้อ |
| **3. Dashboard เต็มรูปแบบ** | หน้าจัดการหัวข้อ, หน้ารายละเอียดข่าว, แก้แคปชัน, อนุมัติ/ปฏิเสธ, Settings, auth | จัดการหัวข้อและข่าวครบทุกสถานะจากหน้าเว็บได้ |
| **4. Facebook Publisher** | ต่อ Graph API, โพสทันที/ตั้งเวลา, เพจปลายทางต่อหัวข้อ, ประวัติโพสต์ | โพสจาก Dashboard แล้วขึ้นเพจจริง พร้อมลิงก์ย้อนกลับ |
| **5. Scheduler อัตโนมัติ** | cron ต่อหัวข้อ + log รอบการดึง | ระบบดึงข่าวทุกหัวข้อเองตามเวลาโดยไม่ต้องแตะ |

---

## 10. ความเสี่ยงและข้อควรระวัง (Risks)

| ความเสี่ยง | ผลกระทบ | แนวทางรับมือ |
|---|---|---|
| Google News RSS เปลี่ยนรูปแบบ/จำกัดการเข้าถึง | ดึงข่าวไม่ได้ | ออกแบบ fetcher เป็น interface กลาง เพิ่มแหล่งอื่น (NewsAPI/GNews) ได้ง่าย |
| Facebook token หมดอายุ | โพสไม่ได้ | ใช้ long-lived Page token + แจ้งเตือนบน Dashboard เมื่อ token ใกล้หมด/ใช้ไม่ได้ |
| keyword กำกวม ได้ข่าวไม่ตรง (เช่น "นางเงือก" เจอทั้งหนัง พะยูน ร้านอาหาร) | คอนเทนต์เพจเสียคุณภาพ | ฟิลด์ aiContext ต่อหัวข้อให้ AI ใช้คัดกรอง + ทุกโพสต์ผ่านการอนุมัติโดยคน |
| หัวข้อยอดนิยม (keyword กว้าง) ได้ข่าวจำนวนมาก | ค่า AI สูง, Dashboard รก | จำกัดจำนวนข่าวต่อรอบต่อหัวข้อ (ตั้งค่าได้), ประมวลผลเฉพาะข่าวใหม่ |
| ข่าวบางวันไม่มีเลย | เพจเงียบ | แสดงบน Dashboard ว่า "วันนี้ไม่มีข่าวใหม่" — อนาคตอาจเสริมคอนเทนต์ evergreen |
| เรียก AI API บ่อยเกินจำเป็น | ชน rate limit ของ Gemini free tier / มีค่าใช้จ่าย | ประมวลผลเฉพาะข่าวใหม่ที่ไม่ซ้ำ, cache ผลต่อ URL, ทำคิวแบบเว้นจังหวะ (throttle) ให้อยู่ใน free tier |

---

## 11. สิ่งที่ต้องเตรียมก่อนถึงแต่ละเฟส (Dependencies)

1. **เฟส 2:** Gemini API key จาก Google AI Studio (https://aistudio.google.com — ฟรี ใช้บัญชี Google)
2. **เฟส 4:** Facebook Page ที่เป็นแอดมิน + แอปใน Meta for Developers + Page Access Token (โหมด development ใช้กับเพจตัวเองได้ ไม่ต้องผ่าน App Review)
3. **เฟส 5:** ตัดสินใจที่รัน — เครื่องตัวเอง (ต้องเปิดเครื่องตอน cron ทำงาน) หรือ cloud (Vercel/Railway)

---

## 12. Open Questions

- [ ] โทน/สไตล์แคปชันของเพจนางเงือกเป็นแบบไหน (ทางการ / กันเอง / มีอีโมจิ) — ต้องการตัวอย่างโพสต์เดิมของเพจเพื่อให้ AI เลียนแบบสไตล์
- [ ] ความถี่ schedule ที่ต้องการจริง: วันละครั้งพอ หรือหลายรอบต่อวัน
### คำถามที่ได้คำตอบแล้ว (Resolved)

- [x] **จะ deploy ที่ไหน** → Vercel + Turso (ตอน dev ใช้ไฟล์ SQLite ในเครื่องผ่าน libSQL)

- [x] **มีหัวข้ออื่นนอกจากนางเงือกที่วางแผนจะใช้เลยไหม** → ยังไม่มี — เริ่มใช้งานจริงด้วยหัวข้อ "นางเงือก" หัวข้อเดียว การทดสอบระบบหลายหัวข้อจะใช้หัวข้อทดลองชั่วคราว
- [x] **หัวข้อทั้งหมดโพสลงเพจเดียวกัน หรือคนละเพจ** → ต้องรองรับได้ทั้งสองแบบ — ยืนยันการออกแบบให้กำหนดเพจปลายทางแยกต่อหัวข้อ (fbPageId ใน Topic) โดยหลายหัวข้อชี้เพจเดียวกันได้
