/**
 * ค่าคงที่ของโหมดแคปชันแบบยาว — ไฟล์ล้วน ไม่ import ฐานข้อมูล
 *
 * ต้องแยกจาก long-form.ts เพราะหน้าเว็บ (client component) ต้องใช้ค่านี้ด้วย
 * ถ้า import จากไฟล์ที่แตะ db เข้าไป จะลาก node:fs เข้า client bundle แล้ว build พัง
 * (เคยพลาดมาแล้วกับ reprocess-policy — ดู src/lib/reprocess-policy.ts)
 */

/**
 * จำนวนข่าวสูงสุดที่เขียนแคปชันยาวได้ต่อการสั่ง 1 ครั้ง
 *
 * แต่ละชิ้นต้อง: แกะลิงก์เว็บจริง + โหลดหน้าเว็บ + เรียก AI ด้วย prompt ที่ยาวกว่าปกติมาก
 * (แนบเนื้อข่าวเต็มได้ถึง 6,000 ตัวอักษร) จึงแพงกว่าการประมวลผลปกติหลายเท่า
 * 5 ชิ้นคือจำนวนที่จบใน 1 request ได้โดยไม่ชน timeout ของ serverless
 */
export const MAX_LONG_FORM = 5;
